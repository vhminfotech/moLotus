query GetThreadList($userId: ID!){
    getThreadList(userId:$userId){
        recipientUser{
            name
            message
            msisdn
            userId
            operator
            messageDate
            threadId
            isGroup
        }
    }
}
