mutation DeleteMessages($threadId: ID!, $userId: ID!, $messageId:[ID!]!){
    deleteMessages(deleteMessageInput: {
        threadId: $threadId,
        messageId: $messageId,
        userId: $userId}){
        messageRes
    }
}