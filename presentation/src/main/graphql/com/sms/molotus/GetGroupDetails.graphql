query GetGroupDetails($groupId: ID!) {
   getGroupDetails(groupId: $groupId) {
     groupName
     groupCreatedDate
     participantsOfGroup {
       id
       mobile
       name
       isAdmin
     }
   }
 }